- title 'Users'[]

%table.list
  %thead
    %tr
      %th= 'Login'[]
      %th= 'Name'[]
      %th= 'Books on loan'[]
  %tbody
  - @users.each do |user|
    %tr
      %td= link_to user.login, edit_user_path(user)
      %td= user.name
      %td= user.loans.size